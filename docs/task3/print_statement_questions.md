# The Print Statement — Question Bank

Answer everything **on paper**, using the cell method from the worksheet: write each output on graph paper, one letter or digit per cell; every **space** gets its own empty cell with a small dot `·`; every `\n` gets its own cell drawn as `↵`.

Remember the golden rules: output is **exactly** what the program wrote — quotes are not printed, `+` glues and never adds a space, and a variable is replaced by its current value.

Write your answers in your notebook. Answers are **not** in this file.

---

# Part A — What Exactly Is Printed?

Write the exact output of each program in cells — every letter, every space, `↵` for every `\n`.

**A1.**

```
print "om namah"
```

**A2.**

```
print "shambo" + "shiva"
```

**A3.**

```
print "shambo " + "shiva"
```

**A4.**

```
print "shambo" + " " + "shiva"
```

**A5.**

```
print "shambo " + " shiva"
```

**A6.**

```
name = "siva"
print "om " + name + " namah"
```

**A7.**

```
age = 12
print "I am " + age + " years old."
```

**A8.**

```
a = 5
b = 3
print a + " and " + b
```

**A9.**

```
marks = 95
print "Score:" + marks + "\n"
```

**A10.**

```
num1 = 2
num2 = 3
num3 = 4
print num1 + ", " + num2 + ", " + num3 + "."
```

**A11.**

```
a = 10
b = 20
c = 30
print a + " - " + b + " - " + c
```

**A12.**

```
num1 = 6
num2 = 7
print num1 + "," + num2 + "."
```

**A13.**

```
print "namah\nshiva\n"
```

**A14.**

```
print "om\n\nshiva"
```

---

# Part B — Does It Match? Fix It.

Each question gives a **required output** (shown in cells) and a program. If the program already produces the required output exactly, write **correct**. If not, write the fixed print statement.

**B1.** Required output:

```cells
om shiva
```

```
print "om" + "shiva"
```

**B2.** Required output:

```cells
19 years old.
```

```
age = 19
print age + "years old."
```

**B3.** Required output:

```cells
The cost of computer is Rs.20000 more.
```

```
cost = 20000
print "The cost of computer is Rs. " + cost + " more."
```

**B4.** Required output:

```cells
Total marks = 87
```

```
marks = 87
print "Total marks =" + marks
```

**B5.** Required output:

```cells
Ravi and Sita
```

```
print "Ravi" + " and " + "Sita"
```

**B6.** Required output:

```cells
Sum is 12.↵
```

```
a = 12
print "Sum is " + a + "."
```

**B7.** Required output:

```cells
5, 10, 15.
```

```
a = 5
b = 10
c = 15
print a + "," + b + "," + c + "."
```

**B8.** Required output:

```cells
om↵
shiva↵
```

```
print "om" + "shiva\n"
```

---

# Part C — Write the Print Statement

Write a print statement that produces **exactly** the required output. Any print that produces it cell for cell is correct.

**C1.** Given:

```
name = "shambo"
```

Required output:

```cells
om shambo namah
```

**C2.** Given:

```
age = 15
```

Required output:

```cells
Anu is 15 years old.
```

**C3.** Given:

```
cost = 250
```

Required output:

```cells
The book costs Rs.250 only.
```

**C4.** Given:

```
a = 6
b = 4
c = a + b
```

Required output:

```cells
The sum of 6 and 4 is 10.↵
```

**C5.** Given:

```
a = 6
b = 4
c = a * b
```

Required output:

```cells
6 times 4 is 24.↵
```

**C6.** Given:

```
name = "Ravi"
marks = 92
```

Required output:

```cells
Ravi scored 92 marks.↵
```

**C7.** Given:

```
num1 = 7
num2 = 8
num3 = 9
```

Required output:

```cells
7, 8, 9.↵
```

**C8.** Given:

```
a = 3
b = 6
c = 9
```

Required output:

```cells
Table of 3: 3, 6, 9.↵
```

**C9.** Required output (no variables given — strings only):

```cells
namah↵
shiva↵
```

**C10.** Given:

```
num1 = 1
num2 = 2
```

Required output:

```cells
1↵
2↵
```

---

When you finish, check yourself against the answer key — and read the **reasoning**, not just the final cells. Count the empty cells in your answer against the key's, one by one.
