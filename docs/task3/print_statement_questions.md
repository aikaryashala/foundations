# The Print Statement — Question Bank

Answer everything **on paper**. Remember the golden rules from the worksheet: output is **exactly** the characters given — quotes are not printed, `+` glues and never adds a space, a variable is replaced by its current value, and `\n` is the Enter character (draw it as `↵`).

When you copy an output into your notebook, put a small dot `·` under every space so you can count them.

Write your answers in your notebook. Answers are **not** in this file.

---

# Part A — What Exactly Is Printed?

Write the exact output of each program — every character, every space, `↵` for every `\n`.

**A1.**

```
print "Good morning"
```

**A2.**

```
print "Rama" + "Lakshmana"
```

**A3.**

```
print "Rama " + "Lakshmana"
```

**A4.**

```
print "Rama" + " " + "Lakshmana"
```

**A5.**

```
print "Rama " + " Lakshmana"
```

**A6.**

```
name = "Sita"
print "Namasthey, " + name + "!"
```

**A7.**

```
age = 12
print "I am " + age + " years old."
```

**A8.**

```
a = 5
b = 3
print a + " and " + b
```

**A9.**

```
marks = 95
print "Score:" + marks + "\n"
```

**A10.**

```
num1 = 2
num2 = 3
num3 = 4
print num1 + ", " + num2 + ", " + num3 + "."
```

**A11.**

```
a = 10
b = 20
c = 30
print a + " - " + b + " - " + c
```

**A12.**

```
num1 = 6
num2 = 7
print num1 + "," + num2 + "."
```

---

# Part B — Does It Match? Fix It.

Each question gives a **required output** and a program. If the program already produces the required output exactly, write **correct**. If not, write the fixed print statement.

**B1.** Required output: `Hello Ravi`

```
print "Hello" + "Ravi"
```

**B2.** Required output: `19 years old.`

```
age = 19
print age + "years old."
```

**B3.** Required output: `The cost of computer is Rs.20000 more.`

```
cost = 20000
print "The cost of computer is Rs. " + cost + " more."
```

**B4.** Required output: `Total marks = 87`

```
marks = 87
print "Total marks =" + marks
```

**B5.** Required output: `Ravi and Sita`

```
print "Ravi" + " and " + "Sita"
```

**B6.** Required output: `Sum is 12.↵`

```
a = 12
print "Sum is " + a + "."
```

**B7.** Required output: `5, 10, 15.`

```
a = 5
b = 10
c = 15
print a + "," + b + "," + c + "."
```

---

# Part C — Write the Print Statement

Write a print statement that produces **exactly** the required output. Any print that produces it character for character is correct.

**C1.** Given:

```
name = "Anu"
```

Required output: `Namasthey, Anu!`

**C2.** Given:

```
age = 15
```

Required output: `Anu is 15 years old.`

**C3.** Given:

```
cost = 250
```

Required output: `The book costs Rs.250 only.`

**C4.** Given:

```
a = 6
b = 4
c = a + b
```

Required output: `The sum of 6 and 4 is 10.↵`

**C5.** Given:

```
a = 6
b = 4
c = a * b
```

Required output: `6 times 4 is 24.↵`

**C6.** Given:

```
name = "Ravi"
marks = 92
```

Required output: `Ravi scored 92 marks.↵`

**C7.** Given:

```
num1 = 7
num2 = 8
num3 = 9
```

Required output: `7, 8, 9.↵`

**C8.** Given:

```
a = 3
b = 6
c = 9
```

Required output: `Table of 3: 3, 6, 9.↵`

---

When you finish, check yourself against the answer key — and read the **reasoning**, not just the final characters. Count the spaces in your answer against the key's, one by one.
