# Equations vs Assignments — Question Bank

Answer everything **on paper**. Remember the golden rules from the worksheet: in computing `=` means "becomes" (right side first with current values, then store into the left name); the left side must be exactly **one box name**; every multiplication is written with `*`.

Write your answers in your notebook. Answers are **not** in this file.

---

# Part A — Allowed or Not?

For each line, write **allowed** if it is a correct computing instruction, or **not allowed** if it is not. If it is not allowed, also rewrite it as a correct instruction with the same meaning.

**A1.**

```
b = a + 3
```

**A2.**

```
a + 3 = b
```

**A3.**

```
a = a * 2
```

**A4.**

```
9 = c
```

**A5.**

```
c = 2b
```

**A6.**

```
c = a * b - 4
```

**A7.**

```
b = 3(a + 1)
```

**A8.**

```
a - 1 = a
```

---

# Part B — Rewrite Maths as Computing

Each line below is written the maths way. Rewrite it in your notebook as a correct computing instruction.

**B1.**

```
c = 2a + b
```

**B2.**

```
a + 5 = b
```

**B3.**

```
c = 3(a + b)
```

**B4.**

```
2a = b
```

**B5.**

```
c = ab + 4
```

**B6.**

```
4(b - 1) = c
```

---

# Part C — Trace the States

For each snippet, write the value of **every variable after every line** (the full state table), not just the final answer.

**C1.**

```
a = 4
a = a + 4
a = a + 4
```

**C2.**

```
a = 3
a = a * a
a = a - 4
```

**C3.**

```
a = 10
a = a / 3
a = a * 5
```

**C4.**

```
a = 5
b = 2 * a
a = b + a
```

**C5.**

```
a = 14
b = a % 4
a = a / b
```

**C6.**

```
a = 2
b = 3
c = a * b
c = c * c
c = c / 4
```

---

When you finish, check yourself against the answer key — and read the **reasoning**, not just the final numbers. A right answer with wrong reasoning will fail you on the next snippet.
